# JV-Link 移行: 列マッピングと変換層(jv_export.py)設計

作成日: 2026-07-21
前提: 実装計画書（`JV-Link移行_実装計画書.md`）の Route B を採用。本書は「実CSVヘッダ」と「JVLinkToSQLite テーブル」の対応、および変換層の設計を定める。

---

## 0. インストール状況の確認結果（実機確認済み）

| 項目 | 結果 |
| --- | --- |
| JV-Link 本体 | **インストール済み**（`C:\Program Files (x86)\JRA-VAN\Data Lab\` → **32bit** 確認） |
| JVLink バージョン | **4.9.0**（JVLinkToSQLite 対応の V4.9.0 と一致） |
| 利用キー | **登録済み** → JVLinkToSQLite の `-m init` は**不要** |
| データ保存設定 | 「保存する（推奨）」有効・保存先 `C:\ProgramData\JRA-VAN\Data Lab` → **キャッシュ有効で再取得が速い** |
| 関連アプリ | TARGET frontier JV / 時系列オッズViewer2 導入済み |
| 実行環境 | Python 3.13・Git・PowerShell/ターミナル・タスクスケジューラ 完備 |

→ 環境面の障害なし。JVLinkToSQLite を配置して `-m exec` するだけで取得可能な状態。

---

## 1. 入力ファイル5種（実仕様）

すべて **cp932（Shift-JIS）**。スコアラーは `encoding='cp932'` で読む（`score_horse_v3.py` の `_read_df`）。変換層も cp932 で出力してドロップイン互換にする。

| ファイル | 用途 | ヘッダ | レイアウト |
| --- | --- | --- | --- |
| `出馬表_<日付>_<race>.csv` | ①出走馬・当日 | あり（複層） | 行0=メタ項目名 / 行1=レース情報値 / 行2=馬列ヘッダ / 行3+=馬行 |
| `過去走_<日付>_<race>.csv` | ①各馬の全出走履歴 | **無し** | 列順は `COLS`（40列）で固定 |
| `坂路_<日付>_<race>.csv` | ①坂路調教 | あり | 1行1調教 |
| `ウッド_<日付>_<race>.csv` | ①ウッド調教 | あり | 1行1調教 |
| `レース結果_<日付>_<race>.csv` | ⑤結果（回顧用） | あり | 1レース1行（集計値） |

過去走の `COLS`（固定列順）:
`年,月,日,回次,場所,日次,レース番号,レース名,クラス名,芝・ダ,トラックコード,距離,コーナー回数,コース区分,馬場状態,馬名,性別,年齢,騎手,斤量,頭数,枠番,馬番,確定着順,着差タイム,人気,単勝オッズ,走破タイム秒,タイムS,補正タイム,通過順1角,通過順2角,通過順3角,通過順4角,上がり3Fタイム,馬体重,父馬名,母馬名,母の父馬名,PCI`

---

## 2. 列マッピング表

JOIN 鍵の要点: **HC/WC（調教）と UM（血統）は馬名を持たず血統登録番号(KettoNum)で紐づく** → 変換層で `KettoNum → 馬名` の解決が必須。SE（馬毎レース情報）は馬名を持つ。

### 2-1. 過去走 → JV

| 過去走列 | 出所テーブル | 備考 |
| --- | --- | --- |
| 年/月/日/回次/場所/日次/レース番号 | NL_RA_RACE（開催・レース識別） | JyoCD→場所名は変換表が必要 |
| レース名/クラス名 | NL_RA_RACE（Hondai/GradeCD/JyokenCD） | |
| 芝・ダ/トラックコード/距離/コーナー回数/コース区分 | NL_RA_RACE（TrackCD/Kyori） | TrackCDから芝ダ・回り・内外を導出 |
| 馬場状態 | NL_RA_RACE（Siba/DirtBabaCD） | |
| 馬名/性別/年齢/騎手/斤量 | NL_SE_RACE_UMA | |
| 頭数 | NL_RA_RACE（SyussoTosu） | |
| 枠番/馬番/確定着順/人気/単勝オッズ | NL_SE_RACE_UMA | |
| 着差タイム | NL_SE_RACE_UMA（着差） | |
| 走破タイム秒 | NL_SE_RACE_UMA（Time） | mm.ss.f → 秒へ変換 |
| 通過順1〜4角 | NL_SE_RACE_UMA（Jyuni1c〜4c） | |
| 上がり3Fタイム | NL_SE_RACE_UMA（HaronTimeL3） | |
| 馬体重 | NL_SE_RACE_UMA（BaTaijyu） | |
| 父馬名/母馬名/母の父馬名 | NL_UM_UMA（3代血統） | KettoNum で JOIN |
| **タイムS** | **派生（要計算）** | 3章 |
| **補正タイム** | **派生（要計算・最難関）** | 3章 |
| **PCI** | **派生（要計算）** | 3章 |

### 2-2. 出馬表 → JV + ②SmartRC + 当日

行1（レース情報, index 0–14 = メタ項目名順）は NL_RA_RACE（前日確定分）または速報 RT_RA_RACE から生成。
行2以降の馬列:

| 馬列ヘッダ | 出所 | 備考 |
| --- | --- | --- |
| 枠番/番(馬番)/馬名S(馬名)/性別/年齢/騎手/斤量 | NL_SE_RACE_UMA（出走予定） | 前日確定 |
| B(ブリンカー)/替(乗替)/減M(減量) | NL_SE_RACE_UMA の各区分 | |
| 父/母/母父/母の母 | NL_UM_UMA（3代血統） | KettoNum で JOIN |
| 間隔 | 過去走から算出（前走との週数） | 変換層で計算 |
| 単勝/人気/複勝人気・下限・上限/馬体重/増減/馬体重増減 | **当日**（速報 RT_SE_RACE_UMA・RT_WH_BATAIJYU） | 前日は空欄で可（現状も空欄あり） |
| **TGX直前/TGX芝短/TGX芝中/TGXダ短/TGXダ中/TGX脚** | **TARGET由来（=補正タイムの条件別ベスト。JV外）** | 3章。TARGETの基準タイム(STX.DAT)に依存。JV単独では再現不可 |

→ 出馬表は **JV（馬・血統・レース）＋ TARGET(TGX=条件別ベスト補正タイム)＋ 当日(単勝・馬体重)** の合成。変換層は JV 部分と当日値を生成し、**TGX は補正タイムと同じく TARGET基準タイム依存**（3章の方針に従う）。

※訂正: 前版で TGX を「②SmartRC由来」としたのは誤り。コード（`score_horse_v3.py:316` 「TGX値=補正タイムの条件別ベスト」）と TARGET公式FAQで、**TGX は TARGET の補正タイム系指数**であることを確認。

### 2-3. 坂路 → NL_HC_HANRO

| 坂路列 | NL_HC_HANRO | 備考 |
| --- | --- | --- |
| 場所（美浦/栗東） | TresenKubun | |
| 年月日/曜日/時刻 | ChokyoDate/時刻 | |
| 馬名 | UM 経由（KettoNum→馬名） | HCは馬名を持たない |
| 性別/年齢 | UM/SE 経由 | |
| Time1〜4（4F/3F/2F/1F 通過） | ハロンタイム | |
| Lap4〜1 | ラップ | **スコアラーは Lap1（最速ラップ）のみ使用** |

### 2-4. ウッド → NL_WC_WOOD

| ウッド列 | NL_WC_WOOD | 備考 |
| --- | --- | --- |
| 場所/コース/回り/年月日/曜日/時刻 | 各対応列 | |
| 馬名/性別/年齢 | UM/SE 経由（KettoNum） | |
| 10F〜1F | 各ハロンタイム | |
| Lap9〜1 | ラップ | **スコアラーは Lap1（無ければ 1F）のみ使用** |

### 2-5. レース結果 → JV + 派生（回顧用）

| 結果列 | 出所 | 備考 |
| --- | --- | --- |
| 年/月/日/場所/R/レース名/条件/クラス/芝ダ/距離/頭数/コーナー回数 | NL_RA_RACE | |
| 天候/馬場状態 | NL_RA_RACE（TenkoCD/BabaCD） | review が使用 |
| 通過順（1〜4コーナー位置）/通過ラップ表記 | NL_RA_RACE（LapTime/CornerInfo） | review が使用 |
| 単勝シェア標準偏差 | NL_O1/H1（票数） | 未使用列（省略可） |
| **レースPCI/前後3F差/上り5F・4F/PCI3 等** | **派生（要計算）** | 3章。review が レースPCI・前後3F差・通過ラップ表記を使用 |

---

## 3. 派生指標の扱い（最重要・PoC失敗の主因）— 再調査で判明

派生指標は **2系統に分かれる**。切り分けが移行方針を左右する。

### 3-A. JV から再現可能（ペース系）

PCI・レースPCI・前後3F差・上り5F/4F は、**レース自身の各ハロンラップ（NL_RA_RACE の LapTime）＋上がり3F から算出**でき、外部基準に依存しない。TARGET定義式を突合してから実装すれば再現可能。

| 指標 | 使用箇所 | 方針 |
| --- | --- | --- |
| PCI（過去走・レース） | scorer 3・review | RAラップ＋上がり3Fから算出。定義式を突合して実装 |
| 前後3F差 / 上り5F・4F | review | RA の前半/後半ハロンから算出 |

### 3-B. TARGET の「基準タイムファイル(STX.DAT)」に依存（JV単独では再現不可）

**再調査の結論**: 補正タイム・TGX・タイムS(系) は、TARGET が内部で自前計算しているのではなく、**外部配布の「基準タイムファイル(STX.DAT)」を読み込んで計算**している（ユーザーの記憶どおり「基準は別ルート」）。

TARGET公式FAQ・マニュアルより確認した事実:

- **基準タイム** = 補正タイム・TGX等の指数を計算する元になる基準タイム値。TARGET起動時に **`STX.DAT`（データフォルダ内、環境設定で変更可）** として読み込まれる。
- **配布元は JRA-VAN ではなく TARGET作者（BLITZ氏）**。**毎週サポートHPで無料公開**され、TARGETの「データ登録 → 基準タイムファイルのダウンロードと登録」で自動DL・登録。通常は月曜夜までに更新。
- **補正タイム** = 基準タイムをもとに距離差・クラス移動を考慮し中心を100で表した指数（100=そのレースの勝ちタイムの平均レベル、1=0.1秒）。
- **TGX** = 同じ基準タイムから計算される補正タイムの条件別ベスト。

→ したがって補正タイム/TGX は **JV-Link の生データだけでは値を再現できない**。基準タイム(STX.DAT)という外部入力と、その（非公開の）計算式に依存する。ここが「TARGETを完全に切れるか」の分水嶺。

| 指標 | 使用箇所 | 依存 | 位置づけ |
| --- | --- | --- | --- |
| 補正タイム（過去走） | scorer 5 | 基準タイム(STX.DAT) | 最重要・TARGET依存 |
| TGX（出馬表・条件別ベスト） | scorer 最高出力30pt | 上と同一（補正タイムのベスト） | 最重要・TARGET依存 |
| タイムS（過去走） | scorer 1 | 基準タイム系（要確認） | TARGET依存の可能性大 |

### 3-C. 補正タイムの計算式は再現可能か —（再々調査の結論: 不可）

「補正タイムの計算式が調査可能か」を追加調査した結論:

- **集約・指数化の考え方は判明**: 補正タイム = 基準タイムを距離差・クラス移動で補正し中心100で表す（1 = 0.1秒）。TGX各列の集計条件も文書化されている（直前 = 過去5走かつ10週以内・同トラックの補正タイム最高値／芝短 = 芝1000-1600mの最高値／遡及期間は馬齢で9〜24ヶ月調整／脚 = 最高値時の脚質 等）。
- **しかし中核の基準タイムは計算式ではない**: 基準タイムは**「レース毎」に、TARGET作者BLITZ氏が障害以外の全レースへ手動で個別設定した主観値**。コース毎の理論par timeではない。加えて距離・クラス移動の**補正ロジックは非公開**。
- 結論: **補正タイム/TGX はJV生データから式で再現できない**。障害は「難しい計算式」ではなく「非公開かつ人手依存の外部データ（BLITZ氏の基準タイム）」。

これにより選択肢が整理される:

- **旧案3（基準タイムを自前生成して補正タイム再現）は成立しない** → 自前par timeで作る指数は定義が別物になり、実質**案2**に一致。
- 現実の分岐は **案1（ハイブリッド）vs 案2（自作指数でJV完結）** の二択。

| 案 | 内容 | TARGET | トレードオフ |
| --- | --- | --- | --- |
| **案1 ハイブリッド** | 補正タイム/TGX列はTARGET（基準タイムファイル読込）で出力し続け、⑤結果・⑦履歴DB・オッズ等をJV-Linkに移す | **ループに残る**（この列を得るにはTARGET実行が必要。①もTARGETが生成し得るため完全排除は不可） | 指数の連続性を維持・手戻り最小。ただし「TARGET脱却」は部分的 |
| **案2 自作指数でJV完結** | 最高出力30ptの土台（補正タイム/TGX）を自作スピード指数へ置換 | **完全排除** | 30ptは配点大。指数定義変更に伴う**再重み付け・ROIバックテスト再検証が必須**。自作の平均タイム辞書 `course_times_full_new.json` を出発点にできる |

補足: PCI等（3-A）は変換層で再計算しスコアラー無改修を維持。案1/案2いずれでも、3-B の指数部分は方針確定後に単独マイルストーンとして検証する。

---

## 4. 変換層 jv_export.py 設計

役割: `race.db`（JVLinkToSQLite出力）から、既存パイプラインが食う5種CSVを**同一形式・cp932**で生成する。

構成案:
- 入力: `race.db` パス、対象レース識別子（日付・場・R）、出力先 `input/`。
- 中核クエリ: レース単位で `NL_RA_RACE ⋈ NL_SE_RACE_UMA ⋈ NL_UM_UMA(KettoNum) ⋈ NL_HC_HANRO/NL_WC_WOOD(KettoNum)` を結合。
- コード変換表: JyoCD→場所名、TrackCD→芝ダ/回り/内外、GradeCD/JyokenCD→クラス名 等の対応辞書を用意（JV-Data仕様書ベース）。
- 派生指標: 3章の関数群（`calc_pci`, `calc_hoseitime`, `calc_times`, `calc_race_pci`）を実装。
- 出力仕様（互換性が要）:
  - 過去走: **ヘッダ無し・COLS順**で出力。
  - 出馬表: **行0メタ項目名／行1メタ値（index0–14）／行2馬列ヘッダ／行3+馬行**の複層レイアウトを再現。TGX・当日値は空欄（後段で付与）。
  - 坂路/ウッド/レース結果: 各ヘッダ付きで出力。
  - 文字コード cp932、改行・カンマ仕様を既存に合わせる。
- 欠損時の挙動: 坂路/ウッドが空ならスコアラーが `[skip]` する仕様なので、空でも生成はする（取得漏れと区別できるようログ出力）。

呼び出し位置: 現状の「TARGET手動書き出し→input/配置」を、`jvlinktosqlite -m exec` → `jv_export.py` → `run_new.py` の連鎖に置換。

---

## 5. スコアラー実使用列（優先度）

再現必須（scorer が参照, 括弧内は箇所数）:
- 過去走: 確定着順(7)・単勝オッズ(9)・人気(7)・走破タイム秒(4)・上がり3Fタイム(4)・通過順3角(4)・着差タイム(4)・補正タイム(5)・PCI(3)・タイムS(1)・馬体重(1)
- 出馬表: 馬名（キー）・馬番・単勝→単勝オッズ・増減→馬体重増減_raw、および TGX系（案2で自作指数に置換／8章）
- 坂路: 年月日・馬名・Lap1（最速）
- ウッド: 年月日・馬名・Lap1（無ければ1F）
- レース結果(review): レースPCI・前後3F差・通過ラップ表記・天候・馬場状態

→ 生JVで即埋まるものが大半。**ボトルネックは派生4指標（補正タイム/PCI/タイムS/レースPCI系）に集約**される。

---

## 6. PoC 突合方針と受入基準

1. JVLinkToSQLite で直近1年を取得（`-m exec`）。
2. 既に手元にある `過去走_20260222_t11.csv` 等（フェブラリーS）と同一レースを変換層で再生成。
3. **列単位で突合**:
   - 生JV由来列（着順・オッズ・タイム・血統・調教Lap1等）＝**完全一致**を要求。
   - 派生列（補正タイム・PCI・タイムS）＝**相関・平均絶対誤差**で評価（完全一致は不要）。
4. 生成CSVで `run_new.py` を回し、既存 `<race>_pred.html` とスコア・買い目が実用上一致するか確認。

受入基準: 生JV列が完全一致し、派生列の近似でスコア順位・買い判定が既存と実質同等（順位入替が許容範囲内）であること。

---

## 7. 未確定事項・次アクション

- [x] **補正タイム/TGX の方針決定** → **案2（自作指数でJV完結・TARGET完全脱却）で確定**（8章）
- [ ] PCI・レースPCI・前後3F差（3-A）の **TARGET定義式の突合と実装**（JV再現可能）
- [ ] タイムS が基準タイム依存か自算可能かの切り分け
- [ ] JV-Data コード変換表（JyoCD/TrackCD/GradeCD/JyokenCD 等）の整備
- [ ] 出馬表の当日値（単勝・馬体重）を前日運用でどう扱うか（空欄許容で確定か）
- [ ] JVLinkToSQLite の setting.xml 対象種別・取得年数の確定（RA/SE/HR/UM/HC/WC/O*/H*・直近何年）
- [ ] 変換層の場所名/コース名の既存表記（東京・中山…／「東京ダート1600m」等）との整合確認

---

## 8. 方針決定と自作スピード指数の設計方針（案2採用）

**決定（2026-07-21）**: 案2（自作指数でJV完結・TARGET完全脱却）を採用。補正タイム/TGX は、⑦履歴DBから算出する**独自基準タイム**を土台にした自作スピード指数で置換する。

### 8-1. 独自基準タイムの算出

⑦履歴DB（JVLinkToSQLite の NL_RA_RACE / NL_SE_RACE_UMA）から、以下のバケット別に代表勝ちタイム（par time）を集計する:

- 次元: **クラス × コース(場) × 距離 × 芝/ダート**（＋必要に応じ 内外・回り）
- 追加次元候補: **開催週（開催回・日次）**、馬場状態
- 代表値: 各バケットの勝ちタイム（または上位数頭平均）の中央値/トリム平均等（外れ値耐性を持たせる）

既存 `course_times_full_new.json`（競馬場×クラス別平均タイム）を出発点に、距離・芝ダ・開催週へ次元拡張する形が自然。

### 8-2. 自作補正タイム/TGX の定義

- **自作補正タイム** = （独自基準タイム − 走破タイム）を距離・クラスで正規化し、中心100・1=0.1秒でスケール（TARGETの指数感覚を踏襲）。
- **自作TGX** = 自作補正タイムの条件別ベスト。**TGXの集計条件は文書化済みなのでそのまま流用可**（直前=過去5走かつ10週以内・同トラック最高値／芝短=芝1000-1600m最高値／芝中=芝1600-3600m／ダ短・ダ中／脚=最高値時の脚質／遡及期間は馬齢で9〜24ヶ月調整）。
- スコアラーは無改修を維持できる（出馬表のTGX列・過去走の補正タイム列を自作値で埋めるだけ）。

### 8-3. 重要caveat: 馬場差・ペース補正の内製化

TARGETの基準タイムは**レース毎**に手動設定されるため、馬場差・ペースを暗黙に吸収している。一方、本方式の**バケット別par**はこれを吸収しない。したがって自作指数には以下を**明示的に組み込む**:

- **馬場差補正**: 既存 `fetch_baba.py`（馬場状態・クッション値・含水率）＋トラックバイアス フェーズ2（前週/前日実測補正）の成果を、走破タイムの補正項として利用。
- **ペース補正**: PCI（3-A で JV から再現可能）を用い、スロー/ハイの偏りを補正。
- 注意: 開催週次元と馬場差補正は情報が一部重複し得るため、二重補正にならないよう設計・検証する。

### 8-4. 改訂ロードマップ（案2版）

1. **⑦履歴DB構築**: JVLinkToSQLite で必要年数を取得（自作基準タイムの母集団確保が主目的）。
2. **独自基準タイム算出**: 8-1 のバケット別parを生成（`baseline_time.py` 等）。
3. **自作補正タイム/TGX実装**: 8-2＋8-3。過去走・出馬表の該当列を自作値で生成。
4. **検証**: 自作指数 vs 旧TARGET補正タイムの相関確認、および**最高出力30ptの再重み付け・単複ROIバックテスト**（`backtest_v2.py` 拡張）。
5. **全面置換**: ①⑤の入力をJV-Link由来に切替、TARGET手動書き出しを廃止。

→ 本方式は **⑦履歴DBが前提**。よって次の一手は「JVLinkToSQLiteで履歴DBを作る（PoC）」に集約される。

### 8-5. 未確定（案2の設計判断）

- [ ] 独自基準タイムのバケット粒度（開催週を入れるか、馬場状態を次元にするか）
- [ ] 代表値の集計方法（勝ちタイム中央値／上位平均／回帰）
- [ ] 馬場差補正の式（fetch_baba値を秒補正へ変換するルール）
- [ ] 履歴DBの取得年数（例: 10年で母集団十分か）

---

## 参考
- JVLinkToSQLite テーブル仕様: https://github.com/urasandesu/JVLinkToSQLite/wiki/Table-Spec
- TARGET補正タイム/基準タイム（公式FAQ・解説）: https://targetfaq.jra-van.jp/faq/detail?site=SVKNEGBV&id=594 / https://team-d.club/speed-index/about-hosei-time-index/
- 実装計画書: `JV-Link移行_実装計画書.md`
