# スコア因子 総点検メタプロンプト（keiba-dashboard）

## 0. このドキュメントの使い方
これは「全スコア因子（〜19項目）のロジックと、ヘッダ／ダッシュボード説明文／設定幅の整合性を総点検する」ための実行指示（メタプロンプト）です。監査を担当するエージェント（または自分自身）は、本書の手順に従い、**推測でなく実コード・実データで検証**し、最後に「因子別 所見表」と「修正パッチ案」を出力します。

対象ファイル（すべて `C:\Users\r-ito\keiba-dashboard`）:
- `score_horse_v3.py` … スコア算出本体（因子計算・総合スコア合算・JSON/CSV出力）
- `build_dashboard_v3.py` … pred HTML（バー表示・凡例 adj-legend）
- `build_review.py` / `bet_recon.py` … 回顧・買い目再構築（pred と同一閾値/ラベルか）
- `analyze_factors.py` … 因子列一覧（FACTORS）
- `course_tenkai_bias.json` … コース特徴pts のバイアス表

大原則: **大きいファイルの編集は Edit ツールを使わない**（末尾切り詰めが再発する）。`score_horse_v3.py`・`build_dashboard_v3.py` は必ず bash 単一プロセス（read→replace→write を1回のpythonで）で編集し、編集後は必ず `ast.parse` と行数、末尾行を確認する。

---

## 1. 監査の目的（ユーザー指定＋追加観点）

### ユーザー指定の3点
1. **死んでいる／誤って固定値になっている因子はないか** … 常に0、分岐が到達不能、合算に入っていない、重み0で無効、後段で上書きされている等。
2. **ヘッダ／ダッシュボード説明文と乖離したロジックはないか** … docstring・adj-legend・バー表示スケールが実装と食い違っていないか。
3. **設定ptsの幅が想定と異なる因子はないか** … 公称±N と 実clip／実効レンジ／偏差値マッピングの上限が一致するか。

### 追加でチェックすべき観点（本監査で必須）
4. **合算漏れ／二重計上** … `総合スコア` の合算式（`score_horse_v3.py` L2230付近）に全因子列が過不足なく入っているか。逆に意味的に重複する因子が二重に効いていないか（例: コース適性pts × コース特徴pts × 馬場適性pts、上がりpts × コース特徴pts末脚成分）。
5. **符号の正しさ** … 減点すべき因子が加点になっていない／その逆。
6. **clip と公称の不整合** … 公称±N なのに clip がそれより広い（外れ値が突き抜ける）／狭い（公称値に到達不能）。
7. **偏差値マッピングの飽和** … `to_points`(dev35→0/上限) や `to_symmetric_points`(dev50中心) が、実データで多数の馬を天井/床に張り付かせていないか（=解像度が死んでいる）。
8. **欠損データ時の既定値バイアス** … 過去走なし／NaN のとき neutral(0/偏差50) か、暗黙にペナルティ/加点になっていないか。σ計算からのNaN除外が正しいか。
9. **重みFACTOR_WEIGHTSの棚卸し** … 0.0の因子（現状 `枠順pts`）は表示上も「無効」と明示されているか。dictに未登録で `fw()`のデフォルト1.0に依存している因子はどれか。
10. **表示解像度** … `コース特徴pts` は `int(round())` 出力で ±5 を整数化（解像度劣化）。他因子は .1 表示。意図的か。
11. **pred/review の一致** … 同じ因子・判定の閾値/ラベルが両系で一致しているか（過去に review 側のペースラベル不具合あり）。
12. **廃止ロジックの残骸** … PCI補正・ペース判定・「展開適性±15pt」等、廃止済みの記述/コメント/変数が残っていないか。
13. **ハードコード前提** … `TARGET_COURSE="東京芝1600m"` / `18頭` など既定値フォールバックが、実運用で悪影響を与えないか。
14. **マジックナンバー** … 閾値定数（例: 400m, 57kg, ±12/20kg, 0.5秒境界）が説明文と一致し、根拠があるか。

---

## 2. 因子インベントリ（3つの真実源を1行で並べる）

監査の第一成果物。下表を**実コードを読んで**埋め直す（下の値は棚卸し時点の暫定・要検証）。列: 因子 / 算出関数・行 / ヘッダ記載(§docstring L5-19) / 凡例記載(adj-legend) / バー表示スケール / 実装のclip・上限 / 重み / 合算式に有無。

| 因子(列名) | 算出箇所 | ヘッダ公称 | 凡例公称 | バー表示 | 実装clip/上限 | 重み | 合算式 |
|---|---|---|---|---|---|---|---|
| 最高出力pts | to_points 30 (L2119) | 30pt | 35pt | /35? | max30 | 1.0 | 有 |
| クラスpts | to_points 25 (L2120) | 25pt | 25pt | /25 | max25 | 1.0 | 有 |
| 時計pts | 独自 (L2124) | **20pt** | 25pt | /25 | max25 | 1.0 | 有 |
| コース特徴pts | calc_tenkai_pts_all clip±5 (L1544) | **展開適性±15pt(旧)** | ±5pt | ±5(int) | ±5 | 1.0 | 有 |
| 斤量pts | calc_kinryo_pts | **0/-1pt** | ±3pt | ±3 | 要確認 | 1.0 | 有 |
| 距離pts | calc_kyori_pts | -2〜+2 | -2〜+2 | (バー無) | -2..+2 | 1.0 | 有 |
| コース適性pts | clip±10 (L1140) | ±10pt | ±10pt | **±5** | ±10 | 1.0 | 有 |
| 臨戦pts | calc_rinsen_pts | -4〜+1 | -4〜+1 | — | 要確認 | 1.0 | 有 |
| 人気補正pts | L2289-2302（合算後に算出） | — | 0〜+3pt | — | 0..+3 | (未登録) | **無（要確認：死因子疑い）** |
| 騎手pts | clip±2 (L1233) | ±2 | ±2 | ±2 | ±2 | 1.0 | 有 |
| 馬体重pts | calc_taiju_pts | **0/-1pt** | ±12-19→-0.5/±20→-1 | — | 要確認 | 1.0 | 有 |
| 継続pts | calc_keizoku_pts | 0/+1 | 0/+1 | — | 要確認 | 1.0 | 有 |
| 着差pts | calc_chakusa_pts | -2〜+1 | -2〜+1 | — | 要確認 | 1.0 | 有 |
| 枠順pts | calc_wakuban_pts | — | ±2pt(残存) | ±2 | ±2 | **0.0(無効)** | 有(×0) |
| 昇級pts | L2170 | — | 0〜-3 | — | 0..-3 | 1.0 | 有 |
| クラス適応pts | clip±3 (L1314) | — | **-2〜+1.5** | — | ±3 | 1.0 | 有 |
| 上がりpts | to_symmetric ±3 (L2127) | **記載なし** | 要確認(凡例に有無) | — | ±3 | 1.0 | 有 |
| 馬場適性pts | clip-2..+3 (L1009) | 記載なし | -2〜+3 | ±3 | -2..+3 | 1.0 | 有 |
| SmartRC評価pts | L2173 | -4.5〜+4.5 | -4.5〜+4.5 | — | 要確認 | 1.0 | 有 |

> 太字＝棚卸し時点で乖離が疑われる箇所。監査で1つずつ潰す。

---

## 3. 静的レビュー手順（因子ごと）
各因子について、以下を順に確認し所見を記録する。
1. **算出関数の実体を読む** … 入力・分岐・戻り値・clip を確認。到達不能分岐、常に同値を返す経路、`try/except` で握り潰して0になる経路を探す。
2. **合算式（L2230付近）に列があるか** … `fw('X')*res['X']` の行を確認。無ければ「死因子」候補。重み0なら「無効（表示のみ）」。
3. **算出タイミング** … 合算より後に算出/上書きされていないか（人気補正ptsの典型）。
4. **3真実源の突合** … ヘッダ docstring（L5-19）／adj-legend（L1779-1849）／バー表示スケール（L455-510付近）と実clip/上限を比較。数値・単位・符号・条件文言まで。
5. **pred/review 一致** … 同因子/判定を `build_review.py`・`bet_recon.py` が別実装で持つ場合、閾値・ラベルが pred と一致するか。

---

## 4. 経験的分布ハーネス（死因子・幅ズレを機械的に炙り出す）
静的レビューだけでは見落とすため、**実レースのスコアCSV/JSONを横断集計**する検証スクリプトを作る（`audit_factors.py` として新規）。手順:
1. `input/done/` にある全レースを `score_horse_v3.py` で再スコア（または既存 `horses_data_*.json` / `scores_*.json` を読む）。
2. 各因子列について、レース横断で次を出力する表を作る:
   - `nonzero率`（0でない馬の割合。**≈0%なら死因子**）
   - `min / max / mean / std`（実効レンジ。**公称より狭い/広いを検出**）
   - `clip到達率`（±上限に張り付く馬の割合。**高すぎれば飽和=解像度死**）
   - `欠損時の値`（過去走なし馬でのその因子値の分布）
3. 総合スコアへの**実効寄与**も出す: 各因子 `fw*pts` の分散、および「その因子を0にした時の順位変動（Kendall τ）」で、実際に効いている因子/効いていない因子を定量化。
4. 結果を `audit_factors_report.md` に保存。§2 の表の「実装clip/上限」「合算」列と突き合わせる。

> 注意: ハーネスは既存の集計基盤（`backtest_v2.py` の EV_DATA 読み込み、`analyze_factors.py` の FACTORS）を再利用してよい。新規に人気/結果を引く必要はない。

---

## 5. 出力フォーマット（監査の最終成果物）

### 5-1. 因子別 所見表
| 因子 | 種別 | 深刻度 | 所見（実装 vs 公称/期待） | 推奨対応 |
|---|---|---|---|---|
| 例: 人気補正pts | 死因子 | 高 | 合算式に無く、合算後に算出。スコア無影響 | 合算に追加 or 「表示専用」と明記 |

- **種別**: 死因子 / 固定値 / 説明乖離 / 幅ズレ / 二重計上 / 符号 / 飽和 / 欠損バイアス / 残骸 / その他
- **深刻度**: 高（スコア/順位に実害）/ 中（表示の誤解）/ 低（コメント整備）

### 5-2. 修正パッチ案
- ロジック修正（例: clip値の是正、合算追加、符号修正）
- 説明文の同期（docstring L5-19、adj-legend、バースケールを実装値へ）
- コメント/残骸の削除（PCI・ペース・展開適性±15 等）
- 各修正は「ユーザー確認が要る設計判断」か「明白な不整合の是正」かを分けて提示。**幅の増減やロジック意味の変更は必ずユーザー合意を取る**（是正の方向だけ提案し、勝手に効きを強めない）。

---

## 6. 既知のリード（監査の起点・すべて未確定＝要検証）
1. **人気補正pts が総合スコアに未合算**（L2289 は合算 L2248 より後、`fw('人気補正pts')*...` 行なし）→ 死因子の疑い。意図的な表示専用か確認。
2. **時計pts**: ヘッダ「20pt」だが実装は max25（L2124）。
3. **コース適性pts**: 実clip±10（L1140）／凡例±10だが、**バー表示は±5**（`course_apt_pts:+d}/±5`）。表示スケール誤り。
4. **最高出力pts**: ヘッダ30 / 凡例35 の不一致（実装 to_points=30, L2119）。
5. **斤量pts・馬体重pts**: ヘッダ「0/-1pt」だが凡例/バーは「±3」「±12-19/±20」。ヘッダが旧仕様。
6. **コース特徴pts(旧展開)**: ヘッダ L9「展開適性±15pt 脚質×想定ペース」が完全に旧仕様（現: course_tenkai_bias, ±5clip, ペース廃止）。
7. **クラス適応pts**: 実clip±3（L1314）だが凡例「-2〜+1.5」。上振れ側の公称と実装clipが不一致。
8. **枠順pts**: 重み0で無効化済みだが、凡例（L1833「±2pt」）・バー（L502）・JSON出力が残存。「無効」表示の要否。
9. **上がりpts**: ヘッダ docstring に項目自体が無い（±3, L2127）。凡例にも有無を要確認。
10. **展開マトリクス／想定ペースパネル**（build_dashboard L930-1046 付近）はタスク#3で廃止予定。総点検では「残骸」として記録のみ（削除はダッシュボード改修で）。

---

## 7. 完了条件
- §2 インベントリ表が実コードの値で全埋め。
- §5-1 所見表に全因子（19項目）が1行以上。
- §5-2 に、少なくとも「死因子/幅ズレ/説明乖離」各カテゴリの是正パッチ案。
- ロジック意味を変える提案は**ユーザー合意待ち**として明示、コメント/表示の同期など明白な不整合のみ即修正候補として提示。
- 監査中に見つかった新たな観点は §1 追加観点に追記して残す。
