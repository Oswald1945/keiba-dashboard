# コードベース 網羅的エラーチェック / 死にロジック監査（2026-06-18）

対象: リポジトリ全 .py。手法: 全ファイル `py_compile` ＋ `pyflakes`（未定義名・未使用import）
＋ デッド関数解析（定義のみ・未呼び出し）＋ 固定パス/握りつぶしexcept/廃止ロジックの目視。

## 総括：**実害のあるバグはなし。** 運用パイプラインは健全。
- 全 .py がコンパイル成功。未定義名・到達不能エラーなし。
- pyflakes の警告の大半は無害（プレースホルダなし f-string＝余分なf接頭辞）。
- 修正した潜在不具合1件、無害なデッドコード（整理候補）、陳腐化した研究スクリプトを検出。

---

## ① 修正済み：build_review.py の既定パスが旧セッション固定（潜在不具合）
- 以前: `UPLOADS='/sessions/jolly-vibrant-goldberg/mnt/uploads'` 等、**存在しない旧セッションの絶対パス**を
  `--result/--horses/--scores` の既定値に使用。
- 影響: `run_new.py` からは明示指定で上書きされるため運用フローは正常だが、**build_review.py を単体実行**すると即失敗。
- 対応: スクリプト基準の相対パス（`input/`・リポジトリ直下）に変更。CSV/HTML両方の回顧生成で回帰確認済み（コミット同梱）。

## ② デッドコード（無害・整理候補。現状は実行に影響なし）
**未呼び出し関数:**
- `score_horse_v3.py`: `course_bonus_modifier()`, `dist_range()`, `calc_pci_pts()`（A-6 PCI補正の廃止に伴い常に0/呼出除去済み）
- `build_review.py`: `_read_df()`（HTML対応で `result_loader.load_result` に置換済み。約50行が残存）
- ※ `course_key_from_row()`/`_est_pop_to_grade()` は `.apply`/`.map` 経由で**使用中**（pyflakesの偽陽性）

**未使用ローカル変数:**
- `score_horse_v3.py:2234` `front`（逃げ+先行数。算出のみ未使用）
- `build_dashboard_v3.py:288` `n_runs`（_trust_badge内、未使用）
- `resample_test.py:180` `result`（subprocess戻り値、未使用）

**未使用import:**
- `score_horse_v3.py:1501` `import numpy as np`（関数内・モジュール先頭と重複）/ `:2507` `import sys`
- `build_review.py:654` `import re as _re2` / `resample_test.py:21` `import os`

**意図的に廃止済み（コメント＋本体残存）:** A-6 PCI補正、斤量増減 従来方式、調教pts直接加算。
→ いずれも「常に0」または呼び出し除去済みでスコアに影響なし。

## ③ 陳腐化した研究スクリプト（運用外・実行すれば失敗）
- `analyze_full_review.py`/`analyze_miryokuari.py`/`analyze_score_abs.py`/`analyze_top36.py`/`analyze_top36_v2.py`
  が **旧セッション固定パス** `/sessions/adoring-focused-galileo/...` をハードコード。実行すれば即失敗。
- これらは今セッションで作成した移植版（`analyze_factors.py`/`exp_value.py`/`verify_course_bias.py`、いずれも `__file__` 相対）に
  実質置き換わっている。**削除 or 移植が望ましい**（運用フローには無関係）。
- 握りつぶし `except:`（return None / pass）も上記研究スクリプトに集中。運用ファイル（score/dashboard/review/run_new）は
  `except Exception`/個別捕捉で適切。

## ④ ロジック健全性（確認済み）
- `FACTOR_WEIGHTS` は全因子1.0（重み0の死に因子なし＝原ロジック維持）。
- `run_new.py` の予想/回顧ゲート（--review）は二度処理修正済みで正常。
- レース結果ロードは CSV/Excel/HTML を `result_loader.load_result` で統一、HTMLは配当も抽出。
- 買い目提案・買いレース判定・危険人気馬バッジ・EVシミュレーターは生成JSの疑似DOM実行で例外なし確認済み。

---

## 推奨アクション（優先度順）
1. （済）build_review 既定パス修正。
2. 陳腐化 `analyze_*.py` 5本の削除 or 移植版への統一（誤実行・混乱防止）。
3. デッドコード整理（未呼び出し関数・未使用import/変数）。実行影響はゼロのため低優先。
   ※ score_horse/build_dashboard は大きくマウント同期の都合で編集に注意が必要なため、慎重に。
