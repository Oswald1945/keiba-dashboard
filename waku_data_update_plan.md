# 枠番別データ 差分更新の方法

## 現状
- `*_10y_win_utf8.csv`（場所別10ファイル）と統合版 `all_venues_10y_win.csv` を作成済み。
- 全10場・直近10年・1着のみ・計34,548件。
- 集計結果は `waku_summary_10y.txt` 参照。

## 差分更新の手順（手動・低頻度でOK）
1. TARGET frontier JVで対象場の「レース検索」を開く。
2. 検索条件→場所を1場のみ選択、確定着順=1着、検索月数は前回取得時からの差分でOK
   （例: 前回が2026.6.15時点なら、次回は「検索終了日指定」で前回の最終日付以降〜直近を指定）。
3. CSV出力 → `<venue>_YYYYMMDD_update.csv` のようなファイル名で保存。
4. UTF-8変換: `iconv -f cp932 -t utf-8 <file> > <file>_utf8.csv`
5. 既存の `<venue>_10y_win_utf8.csv` と結合し、Mカラム等で重複除去（日付+場所+レース名+馬名で一意）。
6. `all_venues_10y_win.csv` を再生成し、`aggregate.py` を再実行して `waku_summary_10y.txt` を更新。

## 推奨頻度
- 1年に1回程度の更新で十分（枠番統計は短期間で大きく変動しないため）。
- レース数が増えるタイミング（年末年始やGW後など）にまとめて更新すると効率的。

## 今後の活用方針
- 枠順ptsの基礎データとして、場所別・芝ダート別・頭数別の枠番バイアスを反映。
- 特に新潟・東京・中山などの大箱コースは外枠（7-8枠）の1着率が内枠より高く、
  小倉・函館などの小回りコースでは差が比較的小さい傾向。
- 頭数によって「枠」に入る馬番数が変わるため、頭数別の集計も枠順ptsの補正係数設計に利用可能。
