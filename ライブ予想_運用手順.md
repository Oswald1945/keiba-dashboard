# ライブ予想 運用手順（当日速報・自動化設計図）

現行スコアラー＝**P3(相対上がり)＋P4(成績重み付きコース適性)** 採用状態。新馬・未勝利は精度検証外（対象から外す運用推奨）。

## 全体フロー
`0. JV-Linkキー確認 → ① データ作成 → ② SmartRC → ③ 馬場 → ④ 採点・ダッシュボード`

---

### 0. 【事前】JV-Linkキー設定の確認（重要・自動化の要注意点）
- **①の前に必ず**：JV-Link設定を起動 → **OK** を押して利用キーを有効化する。
  - これを怠ると、データ取得で `RC=-303（利用キー空値）` 等が発生し、race.db更新が失敗する。
  - 起動は管理者権限不要。設定画面を開いてOKするだけ。
- **自動化メモ**：この手順はGUI操作（JV-Link設定ダイアログのOK）で、現状スクリプト化できていない。
  将来のUI自動化では、JV-Link設定のOK押下を含める（またはキー期限切れ検知→アラート）ことが必要。
  キーは一定期間で失効するため、長時間バッチ（15年取得・大規模再採点）の途中でRC=-303が出たら、
  設定OK後に再実行（読み取りポインタ保持で続きから走る）。

### ① 予想用データ作成 — `run_predict_select.bat`
1. race.db を最新化（jvlink_update.ps1）。
2. `predict_select.py` が「結果未確定＝これから予想できる」会場を番号一覧表示。
3. **会場を番号で選択**（複数可 例 `1,3`）。
4. 会場ごとに**レースを柔軟指定**：`all` / `7-12` / `7,9,11` / `R7-R12`（R付き・全角読点・空白区切りも可）。
5. 確認(y)で、選んだ全レースを `input/` に一括出力（出馬表 / 過去走 / 坂路 / ウッド）。
- **検証チェックポイント（自動化で毎回）**：出馬表頭数=出馬行、過去走41列、相対上がり付与、TGX付与、坂路/ウッド有無。

### ② SmartRC 取得（手動 or 自動）
- `run_predict_dash.bat`(=run_new.py) 実行時に `smartrc_fetch.py <race_id> --out` で**自動取得を試行**。
- 失敗時は手動：`python smartrc_fetch.py --rcode XXXX --out` → `smartrc_{race_id}.json` を生成。
- **自動化メモ**：races/view 取得の安定化（またはSmartRC提供元の入力UI化）が今後の課題。

### ③ 馬場（going）の指定 — `baba_manual.json`（手動が最優先）
- 書式（日付→会場→{芝,ダート,任意で降水mm/天候/クッション値/含水率}）:
  ```json
  { "20260725": { "中京": {"芝":"良","ダート":"良"}, "新潟": {"芝":"稍重"}, "札幌": {"芝":"良","ダート":"良"} } }
  ```
- run_new は `baba_manual.json` の当日値を最優先し、当日の降水/天候から**当日推定馬場**を算出して採点に反映。
- 未指定かつ自動取得も失敗すると暫定「良」で生成し警告（後で baba_manual.json を書いて `--regen YYYYMMDD` で再生成可能）。
- **運用メモ**：チャットで馬場を伝えれば、アシスタントが `baba_manual.json` に記入する運用も可。

### ④ 採点・ダッシュボード生成 — `run_predict_dash.bat`
- `run_new.py` が input/ をスキャン → 馬場・SmartRC適用 → `score_horse_v3` → `build_dashboard_v3` → `{race_id}_pred.html`。
- 生成後、GitHub Pages へ自動公開（shared_urls.txt にURL記録）。
- 回顧（レース結果確定後）：`python run_new.py --review`。

### ⑤ 回顧（レース確定後）— 速報系(RT_)結果を使う当日回顧
- **背景**：JRAの確定成績が蓄積系(NL_)に配信されるのは翌日以降。当日夜に回顧したい場合は**速報系(RT_)**を取得する。速報系データは `RT_SE_RACE_UMA` / `RT_RA_RACE` / `RT_HR_PAY` に入る（蓄積系の `NL_` とは別テーブル）。
- **速報系の有効化（初回のみ、PowerShellでJVLinkToSQLiteフォルダから）**：
  ```powershell
  # 速報系カテゴリを有効化（1回設定すれば以降維持される）
  .\jvlinktosqlite setting -x "//JVRealTimeDataUpdateSetting/IsEnabled" -v "true" -f
  ```
- **毎回（対象開催日を当日に設定 → 取得）**：
  ```powershell
  # 対象開催日を当日(例:2026-07-25)に設定
  .\jvlinktosqlite setting -x "//JVRealTimeDataUpdateSetting/DataSpecSettings/JVDataSpecSetting/JVKaisaiDateKey/KaisaiDate" -v "2026-07-25T00:00:00" -f
  # 取得（蓄積系+速報系）
  .\jvlinktosqlite -m exec
  ```
- **確認**：`python check_results.py 20260725` で各レースが `RT ○/○ → 確定 [RT]` になればOK（`check_results.py` はNL_とRT_の両方を表示）。
- **回顧生成**：`run_review_today.bat` を実行。① 予想レースを再エクスポート（`jv_export` はNL_に結果が無ければ**RT_から結果列を取り込む**フォールバックあり→「レース結果CSV あり[RT(速報)]」と表示）② `run_new.py --review` で回顧HTMLを生成しPages公開。
- **注意**：障害戦（例 新潟9R）は落馬・競走中止で着順が付かず「一部」表示になるが対象外でOK。翌日以降に蓄積系(NL_)へも確定成績が入るので、より完全なデータで再回顧したい場合は `run_new.py --review --force` で再生成可能。
- **ペース系の取得不可表示**：レースのラップ/前後3ハロン（前半3F・後半3F・PCI・前後差・通過/上りラップ）は確定成績(NL_RA_RACE、または速報RTにラップが載る場合)から取得する。速報時点で未配信ならレース概要に「速報回顧では取得不可」を一律表示し、翌日の確定回顧(`--review --force`)で実値に更新される。勝ちタイム・最速上がり3F・払戻・的中判定は速報でも取得可。
- **自動化メモ**：将来は「対象開催日の自動設定＋exec＋check＋review」を1バッチ化。速報系は各レース確定直後に順次配信されるため、全確定を待つならレース終了後しばらく置いてから取得する。

---

## バッチ一覧
| バッチ/スクリプト | 役割 |
|---|---|
| `run_predict_select.bat` | ①更新＋会場/レース対話選択＋一括エクスポート |
| `predict_select.py` | 選択UI本体（会場番号・レース柔軟指定） |
| `run_predict_dash.bat` | ④採点→ダッシュボード（run_new.py） |
| `run_predict_data.bat` | ①の従来版（日付/場/R範囲をプロンプト入力） |
| `check_results.py` | 結果取得状況の確認（NL_蓄積系 / RT_速報系を併記） |
| `run_review_today.bat` | ⑤回顧生成（速報結果で再エクスポート→run_new.py --review） |

## 自動化に向けた残課題
1. **JV-Linkキー確認(0)のGUI操作**：スクリプト化 or キー失効検知→通知。
2. **SmartRC自動取得の安定化**：races/view 取得失敗時のフォールバック整備。
3. **馬場の自動取得 or 入力UI**：fetch_baba の安定化、または当日馬場の一括入力フォーム。
4. **新馬・未勝利の自動除外**：predict_select 側でクラス判定して選択候補から外す（現状は手動で選ばない運用）。
