# BACKLOG — 未着手・保留の課題

分類: [APP]=アプリ化で対応 / [LOGIC]=採点ロジック / [OPS]=運用自動化 / [DATA]=データ整備

## 高優先
- [APP] 管理UIからの「更新→エクスポート→採点→公開」ワンストップ化（現状は3バッチ手動）。
- [APP] 的中精度検証ダッシュボード（期間/会場/クラス/馬場別の軸複勝率・順位相関・キャリブレーション）。
- [APP] 買い判定妙味検証ダッシュボード（券種別的中/回収、確定配当照合）。※妙味は市場に勝てない前提を明記。
- [OPS] 速報系(RT_)取得の自動化: 対象開催日(KaisaiDate)自動設定＋`-m exec`＋`check_results.py`＋`--review` を1バッチ/1ボタン化。全レース確定待ちのタイミング制御も。
- [OPS] SmartRC自動取得の安定化（races/view取得失敗時のフォールバック）。現状は手動が確実。

## 中優先
- [LOGIC] 地方(NAR)→中央の実力換算。現状は「地方=未勝利相当に丸め」の保守対応。将来は地方専用の基準タイム/タイム換算を検討（`スコア項目台帳.md` 地方セクション）。
- [LOGIC] 決め手(脚質)の精度: 現状 JRA脚質区分優先＋通過順位から導出。区分・通過順とも無い時は非表示。閾値(0.33/0.66)の妥当性を実データで検証。
- [DATA] ペース系の確定回顧での実値化確認（速報にラップが載らない場合、翌日NL_で `--review --force`）。
- [OPS] 新馬・未勝利の自動除外（predict_select 側でクラス判定して候補から外す）。現状は手動で選ばない運用。
- [APP] メモ馬の重複統合・タグ・次走出走チェック（出走したら通知/アーカイブ）。

## 低優先 / 検証保留
- [LOGIC] 血統(父/母父)適性ptsの新規因子化。**保留**（世代を遡ると複雑・過学習リスク大とユーザー判断）。やるなら父/母父の単純適性のみで増分ROI/精度を測る。
- [LOGIC] P2(因子の情報冗長性の整理)。情報中立で優先度低。クラス/昇級/クラス適応の役割統合を検討。
- [LOGIC] P4(成績重み付きコース適性)のフル5年検証（`run_rescore_p4full.bat` は用意済、未完走）。
- [DATA] 枠順データ更新計画（`waku_data_update_plan.md`）の反映。

## 恒常的な検証点（ウォッチ）
- 総合の軸複勝率 ≈51% が頭打ち。伸びしろは各因子の**計算ロジックの質**（重み再配分では伸びない）。
- 全因子「pts高→複勝率高」で単調（向きのバグ無し）。差は判別力。
- モデルは市場に妙味で勝てない（10+アプローチで確認済）。精度追求に集中。
