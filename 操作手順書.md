# 競馬予想ダッシュボード 操作手順書
最終更新: 2026-06-17

作業フォルダ: `C:\Users\r-ito\keiba-dashboard\`

---

## 全体フロー

```
【初回のみ】パッケージインストール
          ↓
① TARGET からCSV出力（全レース分）
          ↓
② python run_new.py → SmartRC自動取得 → 予想HTML生成・push
          ↓
【レース後】③ レース結果CSV配置 → python run_new.py --review → 回顧HTML生成・push
```

> **SmartRC自動取得について**
> `run_new.py` 実行時に SmartRC JSON が存在しない場合、自動取得を試みます。
> 成功時はDevTools操作不要。失敗時のみ後述の手動取得（STEP 補足）を実行してください。

---

## 【初回のみ】必要パッケージのインストール

**Git Bash** を開いて実行:

```bash
cd /c/Users/r-ito/keiba-dashboard
pip install -r requirements.txt
```

インストールされるパッケージ: pandas / numpy / openpyxl / pykakasi / requests

---

## はじめに｜コマンドは Git Bash で実行する

Python コマンドは **Git Bash** からのみ実行します（コマンドプロンプトは使用不可）。

```bash
# Git Bash を起動後、作業フォルダに移動
cd /c/Users/r-ito/keiba-dashboard
```

> Git Bash での貼り付けは **Shift+Insert**

---

## STEP ①｜TARGET からCSVを出力する

当日予想したい全レース分、以下の4種類を出力します。

### 出力するファイルと命名規則

| ファイル種別 | 命名形式 | 例 |
|---|---|---|
| 出馬表 | `出馬表_YYYYMMDD_場略称R番.xlsx` | `出馬表_20260621_tk11.xlsx` |
| 過去走 | `過去走_YYYYMMDD_場略称R番.csv` | `過去走_20260621_tk11.csv` |
| 坂路 | `坂路_YYYYMMDD_場略称R番.csv` | `坂路_20260621_tk11.csv` |
| ウッド | `ウッド_YYYYMMDD_場略称R番.csv` | `ウッド_20260621_tk11.csv` |

すべて `input\` フォルダに保存します。

### 場略称一覧

| 競馬場 | 略称 | 競馬場 | 略称 |
|--------|------|--------|------|
| 東京   | tk   | 新潟   | ng   |
| 中山   | ny   | 小倉   | kk   |
| 阪神   | hs   | 福島   | fs   |
| 京都   | kt   | 札幌   | sp   |
| 中京   | ck   | 函館   | hd   |

### TARGET での出力手順

1. TARGET を起動してレースを選択
2. **出馬表**: 「ファイル」→「出力」→「出馬表 Excel出力」→ `input\` に保存
3. **過去走**: Ctrl+A（全馬選択）→「過去成績 CSV出力」→ `input\` に保存
4. **坂路・ウッド**: 調教タブから各々 CSV出力 → `input\` に保存
5. 全レース分繰り返す

**完了後の `input\` フォルダ状態（例: 東京11R・阪神8R）:**
```
input\
├── 出馬表_20260621_tk11.xlsx
├── 過去走_20260621_tk11.csv
├── 坂路_20260621_tk11.csv
├── ウッド_20260621_tk11.csv
├── 出馬表_20260621_hs8.xlsx
├── 過去走_20260621_hs8.csv
├── 坂路_20260621_hs8.csv
└── ウッド_20260621_hs8.csv
```

---

## STEP ②｜予想ダッシュボードを生成する

```bash
cd /c/Users/r-ito/keiba-dashboard
python run_new.py
```

**自動で行われること:**

1. `input\` のファイルをレースIDでグループ化
2. SmartRC評価データを自動取得（`smartrc_{race_id}.json` を生成）
3. スコア計算・HTML生成
4. GitHub Pages に push
5. 完了ファイルを `input\done\` へ移動

**実行ログの例:**
```
[Race] 20260621_tk11
  検出: 過去走, 出馬表, 坂路, ウッド
  [SmartRC] 自動取得成功: 18頭
    [SmartRC] ジャスティンパレス: 評価=B (+2.50pt)
    ...
■ ペース: スローペース想定
  1位予想: ジャスティンパレス  score=72.1  (差し)
  [OK] 20260621_tk11 完了 -> done/ へ移動
```

> SmartRC自動取得が失敗した場合は「補足：SmartRC手動取得」を参照

**オプション:**
```bash
python run_new.py --force  # 生成済みでも強制再生成
```

---

## STEP ③｜回顧ダッシュボードを生成する（レース後）

レース結果CSVを `input\` に追加して実行します。

```
追加するファイル: レース結果_20260621_tk11.csv
```

```bash
python run_new.py --review
```

---

## 補足：SmartRC手動取得（自動取得が失敗した場合）

### 手順概要
1. Chrome で https://www.smartrc.jp/v3/ を開きレースを表示
2. DevTools（F12）→ Network タブ → F5 で再読み込み
3. フィルターに `runners` と入力
4. `runners/view?...` 行をクリック → Payload タブで `rcode`（16桁）をコピー
5. Git Bash で実行:

```bash
python smartrc_fetch.py --rcode 2026062105011211 --out
```

成功すると `smartrc_20260621_tk11.json` が `keiba-dashboard\` 直下に生成されます（`input\` の外）。

### rcode の構造（参考）
```
2026 06 21 05 01 12 11
年   月 日 場 回 日 R
```
- 場コード: 05=東京, 06=中山, 08=京都, 09=阪神, 10=小倉...

---

## 再生成フロー（done/フォルダから）

```bash
python resample_test.py --list              # 利用可能レース一覧
python resample_test.py --date 20260614     # 特定日を全再生成
python resample_test.py --race 20260614_hs11  # 特定レースを再生成
python resample_test.py --all               # 全レース再生成
```

---

## トラブルシューティング

| 症状 | 原因 | 対処 |
|------|------|------|
| `[pred] 生成済 -> スキップ` | JSON が残っている | `--force` を付けて再実行 |
| SmartRC評価がすべて 0.0pt | 自動取得失敗 / JSONが古い | 手動取得（上記「補足」参照） |
| `過去走 or 出馬表がない -> スキップ` | ファイル名の形式が違う | `過去走_YYYYMMDD_場略称R番.csv` の形式か確認 |
| `[ERROR] runners/view エラー` | SmartRCセッション切れ | Chromeで開き直し → rcode再取得 |
| Git Bash で `python` が見つからない | PATH の問題 | Git Bash を閉じて再起動 |
| py_compile エラー（unterminated string） | ファイルトランケーション | `git checkout HEAD -- build_dashboard_v3.py` でリストア |
| Null bytes エラー（score_horse_v3.py） | ファイル破損 | `python3 -c "data=open('score_horse_v3.py','rb').read(); open('score_horse_v3.py','wb').write(data.rstrip(b'\\x00'))"` |

---

## ファイル命名クイックリファレンス

```
input\ に置くファイル（必須）:
  過去走_YYYYMMDD_場略称R番.csv
  出馬表_YYYYMMDD_場略称R番.xlsx

input\ に置くファイル（任意・精度向上）:
  坂路_YYYYMMDD_場略称R番.csv
  ウッド_YYYYMMDD_場略称R番.csv

input\ に置くファイル（回顧用）:
  レース結果_YYYYMMDD_場略称R番.csv

keiba-dashboard\ 直下（SmartRC手動取得時のみ）:
  smartrc_YYYYMMDD_場略称R番.json
```

**場略称:** tk=東京 / ny=中山 / hs=阪神 / kt=京都 / ck=中京 / ng=新潟 / kk=小倉 / fs=福島 / sp=札幌 / hd=函館
