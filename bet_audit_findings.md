# 買い判定ロジック 総点検 — 所見

対象: `build_dashboard_v3.py`(pred JS) の EV/判定、`bet_recon.py`(review) の再構築、`backtest_v2.py`(ROI検証)。
前提: スコア＝的中率（市場同等でよい）／買い判定＝妙味レースの選別（控除率の壁を越える）。

## 1. 現行ロジックの地図
**確率モデル**（pred JS / bet_recon 共通）
- 勝率 = softmax(スコア / T=20)。ただし **各馬の勝率を「市場implied勝率(1/オッズ)×MR=3」で上限キャップ**し再正規化。
- 複勝率 = Harville近似（P2/P3）。
- 採算オッズ = 1 ÷ その買い目の的中率（モデル確率）。的中配当 > 採算オッズ → 期待値プラス（表示・回顧評価用）。

**買い判定ゲート（＝どのレースを買うか）** — 軸A＝モデル勝率1位。srcA＝Aの推定人気順。
- `srcA ≥ 4` → **中穴軸**（無条件）
- `srcA ∈ {2,3}` かつ（推定1番人気がモデル4番手以下 or 優秀穴あり）→ **買い妙味**
- 上位4頭僅差(spread≤4) かつ 軸が2位と僅差(gapA≤2)＝1強不在 → BOXに穴(推定5番↓)含み推定1/2番人気がモデル5番手以下なら **混戦BOX**、でなければ **見送り**
- 上記以外で 見送り かつ 優秀穴(推定5番↓&偏差58+)あり → **穴妙味**
- それ以外 → **見送り**

pred(JS)と review(bet_recon) は **同一閾値で一致**（過去のズレは解消済み）。

## 2. 所見（重大度つき）
| # | 種別 | 重大度 | 所見 |
|--:|---|:--:|---|
| F1 | 死変数/設計 | 高 | **`edge = モデル勝率 − 市場勝率` を L2267 で算出しているが以降一切未使用**。市場implied勝率表 `_mktWin`（1番人気31%…）も用意済み。つまり「直接的な妙味(edge/EV)」の材料はあるのに、判定は**人気順ヒューリスティック**で代用している。 |
| F2 | 設計/幅 | 高 | 勝率の **MR=3 オッズキャップ**が、モデル勝率を市場の最大3倍までに制限＝**検出できる妙味の上限を自ら固定**。妙味を狙う目的と逆行。採算オッズ(=1/モデル勝率)も市場寄りに歪み、EV評価が市場に引っ張られる循環。 |
| F3 | 判定 | 中 | **中穴軸(srcA≥4)は無条件**でEV/妙味チェックなし。堅い4番人気（短オッズ）でも中穴軸になり得る＝妙味の裏付けがない。 |
| F4 | マジックナンバー | 中 | 閾値多数（srcA≥4, spread≤4, gapA≤2, dev≥58, ANA_POP≥5, P2≥0.30, dv≥48, D_CLEAR=5, D_CLOSE=2…）が**全データ未検証**。埋め込みの根拠は122Rのみ。 |
| F5 | 検証不足 | 中 | コード内コメントの過去検証「122R: 買い妙味 軸単勝110% / 見送り58%」は**サンプル小**。全259R・現行スコアで判定別ROI・的中率を再検証すべき。 |
| F6 | 二重性 | 低 | `backtest_v2.py` の判定再構築は**旧ロジック**（BOXが `p<0.18`、穴妙味なし）。現行 bet_recon と不一致。検証基盤を現行に合わせる必要。 |

## 3. 核心的な論点（スコアと買い判定のベストバランス）
- スコア＝的中率は「市場同等」でよい（分析済: 総合スコアは市場相似0.73＝十分正確）。
- 問題は**買い判定が“妙味”を直接測っていない**こと。妙味＝(モデル勝率 × オッズ) > 1、すなわち edge>0。今その edge を捨てて人気順で近似している。
- あるべき姿: **正確なスコア→確率**（的中率）× **edge/EVゲートで妙味レースだけ買う**（控除率超え）。F1/F2 はまさにこのゲートの心臓部の欠落・抑制。

## 4. 次の一手（提案）
1. **全259Rで判定別ROIを現行ロジックで再検証**（backtest を bet_recon 準拠に更新）。買い妙味が本当に単勝100%超か、各判定が控除率を越えるかを確認。
2. その上で **直接EV/edgeゲート**の導入を検討（軸/フォーメーションの合成EV>1で買い、閾値はROIで較正）。
3. **MR=3キャップの再検討**（緩和/撤廃でROIが改善するか、過剰自信で悪化するかを検証）。

---

## 5. 実データ検証（全259R / 現行bet_recon準拠）

### 5-1. 軸単勝・複勝ROI（259R）
| 判定 | R数 | 軸勝率 | 軸複勝率 | 軸単勝ROI |
|---|--:|--:|--:|--:|
| 買い妙味 | 39 | 21% | 41% | **90%** |
| 中穴軸 | 41 | 7% | 37% | 34% |
| 混戦BOX | 3 | 0% | 33% | 0% |
| 穴妙味 | 89 | 34% | 61% | 88% |
| 見送り | 87 | 33% | 63% | 72% |

→ **軸単勝ではどの判定も100%未満**。埋め込みコメントの「買い妙味110%」は122R小サンプルの上振れで、全259Rでは90%。ただし穴妙味/BOX/中穴軸は本来フォーメーションが主戦のため軸単勝では過小評価。

### 5-2. フォーメーション別ROI（払戻あり136R / eval_race で実配当照合）
| 判定 | R数 | 100%超の券種 | 100%未満の券種 |
|---|--:|---|---|
| 買い妙味 | 22 | **馬単111%** | 単勝86/ワイド81/馬連50/三連複41/三連単10 |
| 中穴軸 | 23 | **三連単203/馬連187/馬単149** | 複勝96/三連複89/ワイド76/単勝23 |
| 混戦BOX | 1 | （サンプル過少） | — |
| 穴妙味 | 45 | **なし** | 馬連82/ワイド74/三連複47 |
| 見送り | 45 | なし（正しく妙味なし） | 単勝78/ワイド66/三連複62… |

### 5-3. 結論
- **価値の在り処は特定の判定×券種に限定**: 買い妙味×馬単(111%)、中穴軸×馬連/馬単/三連(149〜203%)。ただし**高分散**（点数少・大穴的中依存、n=22〜23）で頑健性は要追試。
- **穴妙味は妙味なし**: 45Rで全券種100%未満（馬連82/ワイド74/三連複47）。**高頻度で発火するが価値を生んでいない**＝判定条件が緩すぎる/誤設計の疑い（重点見直し候補）。
- **見送りは正しく妙味ゼロ**（全券種100%未満）＝ゲートの下側は機能。
- 全体として、現行ゲートは**単勝的中率での妙味は出せておらず**、価値は多点・高配当券種の裾に偏在。F1(edge未使用)・F2(MRキャップ)の是正で「妙味の直接検出」を強化する余地が大きい。

### 5-4. 次段階の候補（F1/F2 判断材料）
1. **穴妙味の条件厳格化**（偏差58→上げ / 推定人気条件 / 三連複相手の絞り）で妙味を回復できるかROI再測。
2. **直接EVゲート**（軸/フォーメーションの合成EV>1 を発火条件に）を現行ヒューリスティックと並走比較。
3. **MR=3キャップ緩和**で買い妙味×馬連などの中位券種ROIが改善するか検証。
